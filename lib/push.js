'use strict';

const log = require('ms-utilities').logger;

const fns = {};


/**
 * Send a push message to a device
 * @param message
 * @param next
 * @returns {Promise.<T>}
 */
fns.sendPush = (message, next) => {


};


module.exports = fns;
